{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capacidades por defecto",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
